{
  "name": "reclaw",
  "version": "0.1.0",
  "description": "CLI for extracting core memories from AI chat exports",
  "type": "module",
  "bin": {
    "reclaw": "bin/reclaw.js"
  },
  "files": [
    "bin",
    "dist"
  ],
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "rm -rf dist && bun build --target node --outfile dist/index.js src/index.ts && mkdir -p dist/prompts && cp prompts/*.md dist/prompts/",
    "prepack": "bun run build",
    "test": "bun test src",
    "test:watch": "bun test --watch src",
    "lint": "bun run lint:biome && bun run lint:typecheck",
    "lint:biome": "biome check .",
    "lint:typecheck": "tsc --noEmit",
    "fix": "biome check --write ."
  },
  "dependencies": {
    "@clack/prompts": "1.0.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.3",
    "@types/bun": "1.2.5",
    "@types/node": "25.3.0",
    "typescript": "5.9.3"
  }
}
